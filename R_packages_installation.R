# Install the core tidyverse, i.e. packages that you’re likely 
# to use in everyday data analyses. 
install.packages("tidyverse")

# Install the magrittr package which offers a set of operators 
# to make your R code more readable
install.packages("magrittr")

# package to simplify and expedite the process of package development 
# (and installation)
install.packages("devtools")

# functions to import data from MS Excel proprietary format files
install.packages("readxl")

# to check potential data quality  problems
install.packages("dataReporter")

install.packages("janitor")
